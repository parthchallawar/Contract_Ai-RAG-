# Quickstart

This file is superseded by [README.md](README.md) — it previously described an
older, mock-AI version of this app (port 3000, no real retrieval, no persistence).

See **README.md → Setup** for the current install/run instructions (Node 20+,
`npm install`, `.env` from `.env.example`, `npm start` → port 8080), and
**ARCHITECTURE.md** for how the pipeline actually works.
